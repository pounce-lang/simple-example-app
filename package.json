{
  "name": "rollup-starter-app",
  "devDependencies": {},
  "dependencies": {
    "@pounce-lang/core": "^1.1.1",
    "@rollup/plugin-commonjs": "^11.0.2",
    "@rollup/plugin-node-resolve": "^7.1.1",
    "date-fns": "^2.12.0",
    "rollup": "^2.6.0",
    "serve": "^11.3.0"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  }
}
